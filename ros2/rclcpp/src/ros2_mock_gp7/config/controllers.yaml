# GENERATED BY GEMINI AND NO CHECK YET

controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    # 1. 関節状態を配信するコントローラ
    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    # 2. 軌道制御を行うコントローラ (Action Server)
    joint_trajectory_controller:
      type: joint_trajectory_controller/JointTrajectoryController

joint_trajectory_controller:
  ros__parameters:
    joints:
      - joint_1_s
      - joint_2_l
      - joint_3_u
      - joint_4_r
      - joint_5_b
      - joint_6_t

    command_interfaces:
      - position
    state_interfaces:
      - position
